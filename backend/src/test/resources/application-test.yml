spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

jwt:
  secret: test_jwt_secret_key_for_unit_tests_only_32_bytes_long!!
  expiration-ms: 86400000

app:
  upload:
    dir: ${java.io.tmpdir}/maansarovar_test_uploads
  admin:
    initial-username: testadmin
    initial-password: testadminpassword123
    initial-email: testadmin@maansarovar.com
